node_modules/
dist/
.next/
.turbo/
.env
.env.local
.env.*.local
.env.production-backup
*.log
.DS_Store
coverage/
.vercel

# Local Claude Code config (agents, skills, worktrees, plans, etc.)
.claude/
tsconfig.tsbuildinfo
*.tsbuildinfo

# Storage local de PDFs subidos (dev). En prod van a R2 — nunca al repo.
apps/api/uploads/
